# This directory contains the internal scripts we use to quickly evaluate all tasks.
# Fully automated evaluation can be platform-dependent, so we cannot guarantee that these scripts will work unchanged on every user's setup.
# We release them mainly as additional references. For the core evaluation workflow, please refer to `examples/LIBERO/README.md`.